# Project Instructions

> Global workflow, testing and documentation rules already apply 
> from `~/.claude/CLAUDE.md` — this file only needs project-specific detail.

## Project Goal

<!-- ============================================================ -->
<!--  EDIT ME per project. Update as the project evolves.         -->
<!-- ============================================================ -->


## Stack & Code Style

- Language / framework:
- Package manager: pnpm (available via Corepack in this container) — use
  `pnpm add`/`pnpm install`/`pnpm run` rather than the npm equivalents.
  Change this line if this project should use npm or yarn instead (e.g. an
  existing project with a `package-lock.json` you don't want to migrate).
- Formatting / linting tool (e.g. Prettier, ESLint config):
- Preferred patterns (e.g. functional components, no default exports):
- Naming conventions:

## Git / Commits

- Commit message style (e.g. Conventional Commits):

## Project-Specific Overrides

<!-- Only fill this in if this project needs to deviate from the global
     "always ask about auth/payments/migrations" defaults, e.g. a
     prototype where you want Claude to move faster and ask less. -->