Before writing or changing any code for the following task, produce a
numbered, concrete implementation plan and post it back to me. Do not start
implementing anything yet — wait for me to confirm the plan or request
changes to it first.

Once I confirm the plan:
- Write (or update, if one already exists) `PROGRESS.md` at the project
  root with the confirmed plan and its steps marked pending, per the
  "Session Handoff / Progress Log" section of the global instructions.
- Work through the plan one step at a time using your task-tracking so I
  can see progress, pausing after each step for a brief summary before
  moving on (per the global workflow instructions).
- Update `PROGRESS.md` as each step completes, so the file always reflects
  current state, not just the plan as originally confirmed.

Task: $ARGUMENTS